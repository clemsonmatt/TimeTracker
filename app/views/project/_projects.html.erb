<table class="table table-striped table-hover">
    <% if @projects.to_s.length > 0 %>
        <% @projects.each do |proj| %>
            <tr>
                <td>
                    <h4 class="list-group-item-header" style="line-height: 20px;">
                        <%= proj.title %>
                    </h4>
                    <% if proj.description %>
                        <p class="list-group-item-body text-muted" style="line-height: 15px;">
                            <%= proj.description %>
                        </p>
                    <% end %>
                </td>
                <td class="text-center" style="width: 160px;">
                    <% if proj.status == 'locked' %>
                        <h4>4 entries</h4>
                        <center class="text-muted">
                            <%= material_icon.lock %>
                        </center>
                    <% else %>
                        <h4>4 entries</h4>
                        <a href="" class="btn-round btn-round-default btn-round-sm pull-left" style="margin-right: 9px">
                            <%= material_icon.view_list.md_18 %>
                        </a>
                        <a href="" class="btn-round btn-round-warning-simple btn-round-sm pull-left" style="margin-right: 9px">
                            <%= material_icon.edit.md_18 %>
                        </a>
                        <a href="" class="btn-round btn-round-danger-simple btn-round-sm pull-left" style="margin-right: 9px">
                            <%= material_icon.archive.md_18 %>
                        </a>
                        <a href="" class="btn-round btn-round-grey-simple btn-round-sm pull-left" style="margin-right: 9px">
                            <%= material_icon.lock.md_18 %>
                        </a>
                    <% end %>
                </td>
            </tr>
        <% end %>
    <% else %>
        <tr>
            <td>
                None
            </td>
        </tr>
    <% end %>
</table>
